import React from 'react';

import simpleStyles from '../../commons/simple.module.sass';

export default () => (
    <section className={simpleStyles.content}>
        <h1>Gracias por el contacto!</h1>
        <p>Nos vamos a estar comunicando con vos pronto.</p>
    </section>
);